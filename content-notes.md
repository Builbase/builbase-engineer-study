# 勉強会の話す内容

このファイルを内容の正本とする。
`slides.md`は、ここにある主張を1枚1メッセージへ圧縮したもの。

## 0. この勉強会で持ち帰ってほしいこと

学生エンジニアが、道具の名前ではなく役割で選べるようになること。
AIエージェントを増やすだけでは速くならず、独立した仕事へ分けられたときに並行作業が役立つと理解すること。

質問は最後まで待たず、その場で歓迎する。
本編は55分で終え、最後の5分を質疑に使う。
VS Code、Cursor、Ghostty、cmux、Herdrの比較は、第1章の流れで扱う。

## 1. ターミナルとエディタ

### 1-1. この勉強会では3つの役割で考える

よくある混乱は、エディタ、ターミナル環境、CLIエージェントを同じ種類として比べることから始まる。

- エディタ: ファイル、プロジェクト構造、差分、診断を見る作業空間
- ターミナル環境: 文字でコマンドを渡し、出力を受け取る作業空間
- CLIエージェント: 調査、編集、コマンド実行を代行するプログラム

VS CodeやCursorはエディタ。
Ghosttyはターミナルアプリ、cmuxはターミナルと作業空間をまとめたmacOSアプリ、Herdrはターミナル内で動くセッション管理ツール。
Claude CodeやCodexはCLIエージェント。
厳密な製品分類ではなく、初心者が役割を整理するための見方として紹介する。

### 1-2. ターミナルとは何か

MIT Missing Semesterの講義映像では、ターミナルに表示されたシェルへ命令を入力し、プログラムを起動する流れを実演している。

ターミナルは、文字による入力と出力を扱う窓。
シェルは、入力を解釈して`git`、`npm`、`python`などのプログラムを起動する。
この2つを同じものとして説明しない。

話す順番:

1. 人がターミナルへ文字を入力する
2. シェルが命令を解釈する
3. プログラムが実行される
4. 結果がターミナルへ返る

ターミナル上のコマンド操作は、同じ手順を文字として再現し、履歴やスクリプトへ残しやすい。
弱みは、現在地や対象を自分で把握する必要があり、慣れないうちは誤操作の範囲を見誤りやすいこと。

### 1-3. エディタとは何か

VS Codeの公式入門映像では、フォルダを開き、ファイルを移動し、編集し、検索やターミナルを使う一連の流れを示している。

エディタは、ファイルとプロジェクトの構造を見ながらコードを読み書きする作業空間。
補完、定義移動、診断、差分表示により、文脈を画面上で追いやすい。
VS CodeやCursorには統合ターミナルもあり、エディタとターミナルは排他的ではない。

エディタの強みは、構造、変更、エラーの位置を視覚的に追えること。
弱みは、拡張や機能を増やしすぎると画面と設定が複雑になること。
複数の長時間セッションを一覧したい場合は、エディタ単体より専用の管理機能が向くこともある。

### 1-4. どちらを使うかではなく、何へ注意を向けるか

コードを読み、差分を比べ、エラー箇所を直すときはエディタが向く。
複数のコマンド、サーバー、AIセッションの状態を追うときはターミナル環境が向く。
実務では、同じプロジェクトを両方から開く形が自然。

### 1-5. VS CodeとCursor

VS Codeの強み:

- 拡張機能と対応言語が広い
- Remote DevelopmentやDev Containersなど、開発環境の選択肢が多い
- Copilotとエージェント機能を組み合わせられる

VS Codeの弱み:

- AI体験は拡張、設定、利用プランの組み合わせに左右される
- 機能が多く、初心者は必要なものを選ぶまでに時間がかかる

Cursorの強み:

- コードベースを前提にしたAI操作が中心に置かれている
- 複数箇所の編集やエージェント操作へ早く入れる
- VS Code系の操作感を引き継ぎやすい

Cursorの弱み:

- AI機能の利用枠や料金を確認する必要がある
- 製品固有のワークフローへ依存しやすい

どちらも進化が速い。
「VS Codeでは複数エージェントを動かせない」のような固定的な言い切りはしない。

### 1-6. Ghostty、cmux、Herdr

#### Ghostty

Ghosttyは、高速でネイティブな汎用ターミナル。
GPU描画に対応し、タブ、分割、複数ウインドウを標準で扱える。
macOSとLinuxで使えるが、Windows版はまだ提供されていない。

普段の開発や、少数のCLIセッションを軽快に扱う場面に向く。
AI専用の状態一覧は持たないため、入力待ちのセッションはタブや分割から自分で追う。

#### cmux

cmuxはGhosttyを基盤にしたmacOS用ターミナルで、複数のターミナルやAIセッションを1つの作業空間へまとめやすい。
縦タブには作業フォルダ、Gitの状態、通知などが表示される。
エージェントが入力を待つと、ペインの色と通知から戻る場所を見つけられる。
ブラウザをターミナルと並べ、開発中の画面も同じ作業空間で確認できる。

一方、cmuxはmacOS専用。
公式が説明するように、cmuxは仕事の進め方を自動で決める製品ではない。
誰に何を任せるかは、人が設計する。

#### Herdr

Herdrは、バックグラウンドのサーバーがターミナルセッションを維持する。
Herdrサーバーが動いている間は、クライアントを閉じたり接続が切れたりしても処理が続く。
別のターミナルやSSHから再接続できる。
対応するAIエージェントでは、working、blocked、idleなどの状態も確認できる。

長時間のAI作業や、別端末から作業へ戻る場面に向く。
ただし、サーバーの起動とdetach／reattachを覚える必要がある。
Herdrサーバーが再起動すると、任意の実行中プロセスそのものは戻らない。
対応するAIエージェントでは会話を再開できる場合があるが、未対応のツールでは詳細な状態表示や復元を使えない。

選び方は、装飾よりも次の3点で決める。

1. 何本のセッションを追うか
2. ローカル中心か、遠隔・再接続が必要か
3. 人の入力待ちを一覧で見つけたいか

### 1-7. Claude CodeとCodex

Claude CodeとCodexは、ターミナルで動くCLIエージェント。
リポジトリを調査し、ファイルを編集し、コマンドを実行できる。

ただし、AIが「完了」と報告したことは正しさの証明ではない。
人は差分、テスト、ログ、残課題を確認する。
権限要求、外部通信、秘密情報、破壊的コマンドは特に慎重に扱う。

### 1-8. 状態表示の読み方

`running`は、処理中であること。
`needs input`や承認待ちは、人が次の入力を返す必要があること。
`idle`は、現在処理していないこと。
どの状態も、要求を正しく満たしたことまでは保証しない。

見るべき指標は、AIエージェントがいくつ動いているかではない。
次に人の注意が必要な場所と、検証済みの成果がいくつあるか。

## 2. 複数のAIエージェントを動かす

### 2-1. この勉強会で扱う2つの進め方

Aは、違うプロジェクトに1つずつセッションを開く方法。
Bは、1つのプロジェクトを複数の担当へ分ける方法。

Aではファイル競合が起きにくい。
Bでは境界設計、作業場所、統合、検証が必要になる。
どちらも複数のエージェントを使うが、Bのほうが統合の設計を必要とする。

### 2-2. A: 違うプロジェクト

やることは、プロジェクトごとに別のセッションを起動し、独立した指示を渡すこと。
AIが調査やテストをしている待ち時間に、別のプロジェクトを確認できる。

ボトルネックは計算時間から、人の注意へ移る。
優先する順番は次の通り。

1. 承認や追加情報を待っているもの
2. 失敗し、進めなくなっているもの
3. 結果が出てレビューできるもの
4. 自律的に処理中のもの

「処理が終了した」と「作業が完了した」を分ける。
処理終了はセッションの状態。
作業完了は、完了条件を満たし、検証結果があり、人が受け入れられる成果になったこと。

### 2-3. B: 1つのプロジェクト

最初に、全員が読む設計メモを作る。
学生向けには「契約」ではなく「設計メモ」と呼ぶ。

Todoアプリなら、次の4点を1枚にまとめる。

- どんな画面を作るか
- 何ができるアプリか
- 担当間でどのデータを受け渡すか
- どこまで動けば完成か

AIへいきなり「全部作って」と頼むと、各担当が別の前提で作り始める。
共通の設計メモを読ませてから、担当を渡す。

### 2-4. Todoアプリの分解例

Todoアプリを4つの担当に分ける。

- **UI担当**：画面を作る
- **バックエンド担当**：機能とAPIを作る
- **DB担当**：データを保存する
- **レビュー担当**：できた成果から確認し、最後に組み合わせる

役割を分けるだけで、4つのセッションを一斉に動かすわけではない。
UI、バックエンド、DBのうち、互いを待たない作業だけを同時に進める。
レビュー担当は、成果が届くたびに担当範囲と完了条件に沿って確認する。
3担当がそろうまで待たず、問題があれば早めに担当へ返す。

たとえばUI担当は、仮のデータを使って画面を先に作れる。
一方、バックエンド担当がDBの形を必要とするなら、DB担当の決定を待つ。

### 2-5. 担当ごとのゴール

「UI担当」や「バックエンド担当」という名前だけでは、終了条件が分からない。
担当ごとに、何を作り、どの確認まで行うかを決める。

- UI担当：Todo一覧と追加フォームを作り、仮のデータで画面を確認する
- バックエンド担当：追加、取得、更新の機能を作り、APIのテストを実行する
- DB担当：Todoを保存して読み出し、データが残ることを確認する

### 2-6. AIへ渡す4項目

学生が最初に使う指示は、次の4項目から始める。

```text
やること：Todo一覧画面を作る
変更する場所：src/components/の中
変更しない場所：src/api/とsrc/db/
完了条件：画面を表示でき、テストが通る
```

実務では、使用する型、API、報告形式なども足す。
最初から項目を増やすより、担当と終了条件を外さないことを優先する。

### 2-7. 同じファイルを触らせない

UI担当とバックエンド担当が同じファイルを同時に編集すると、変更がぶつかる。
対策は次の3つ。

1. UI、API、DBの担当場所を分ける
2. 変更が重なるなら、先に一方を終わらせる
3. 小さな修正は1つの担当へまとめる

Gitに慣れた参加者から質問が出たら、branchやworktreeも紹介する。
worktreeは作業中の編集を隔離できるが、最後のmerge競合まで消すものではない。
本編では、担当場所を分ける考え方を先に理解してもらう。

### 2-8. レビュー担当の仕事

レビュー担当は、UI、バックエンド、DBの成果が届くたびに差分と動作を確認する。
問題は早めに担当へ返し、最後に成果を組み合わせて全体を動かす。
確認する内容は3つに絞る。

1. 担当範囲と完了条件を満たしているか
2. APIとデータ形式の約束がそろっているか
3. 組み合わせた状態で全体テストが通るか

問題を見つけたら、該当する担当へ根拠と一緒に返す。
レビュー担当が一人で全部を書き直すと、役割が再び混ざる。

別のAIが「大丈夫」と言っただけでは完成ではない。
人が差分と実際の動作を確認して、完成を判断する。

### 2-9. うまくいかないとき

最初に確認する失敗は3つ。

- 同じファイルを直した：担当場所を分ける
- UIとAPIの形が違った：設計メモをそろえる
- レビューが最後に集中した：届いた成果から小さく確認する

迷ったら、同時に動かす数を減らす。
1つのセッションで順に進めたほうが速い作業もある。

## 3. 最後に伝える3行

この勉強会では、道具をエディタ、ターミナル、CLIエージェントの3つの役割で考える。
AIエージェントには、UI、バックエンド、DB、レビューの担当範囲を文章で渡す。
独立した仕事だけを並行し、最後は人が差分と動作を確認する。

## 4. 主な出典

- MIT Missing Semester, The Shell: https://missing.csail.mit.edu/2020/course-shell/
- MIT講義映像: https://www.youtube.com/watch?v=Z56Jmr9Z34Q
- VS Code入門映像: https://www.youtube.com/watch?v=f8_uF_IDV50
- VS Code Docs: https://code.visualstudio.com/docs
- Cursor Docs: https://docs.cursor.com/
- Ghostty Docs: https://ghostty.org/docs
- cmux: https://github.com/manaflow-ai/cmux
- Herdr Docs: https://herdr.dev/
- OpenAI Subagents: https://learn.chatgpt.com/docs/agent-configuration/subagents
- OpenAI Git worktrees: https://learn.chatgpt.com/docs/environments/git-worktrees
- OpenAI Long-running work: https://learn.chatgpt.com/docs/long-running-work
- Anthropic Agent teams: https://code.claude.com/docs/en/agent-teams
- Anthropic Worktrees: https://code.claude.com/docs/en/worktrees
- Anthropic Multi-agent research system: https://www.anthropic.com/engineering/multi-agent-research-system
- Git worktree: https://git-scm.com/docs/git-worktree
