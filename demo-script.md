# 実演デモ台本

60分の勉強会で2回入れる。
本番前に必ず通し、同じ解像度でスクリーンショットも用意する。

## 共通の準備

- [ ] 画面共有時のターミナル文字を18pt以上にする
- [ ] 通知、メール、チャットのプレビューを切る
- [ ] 秘密情報、顧客名、個人情報が画面に出ないデモ用リポジトリを使う
- [ ] `git status`で意図しない変更がないことを確認する
- [ ] AIツールへログインできることと利用枠を確認する
- [ ] ネットワーク障害に備え、各デモのスクリーンショットを用意する
- [ ] 書き込みを伴う場合は、使い捨てbranchまたはworktreeを使う

デモでは速さを競わない。
画面上のどの証拠を見て判断したかを、声に出して示す。

## 実演1: 同じプロジェクトを2つの窓で見る

挿入位置は9枚目のあと。
所要時間は3分。

見せたいことは、エディタとターミナルの役割が違い、同時に使えること。

### 手順

1. エディタで小さなデモ用プロジェクトを開く
2. ファイルツリー、定義移動、診断、差分表示を順に指す
3. 統合ターミナルまたは別のターミナルで`pwd`と`git status`を実行する
4. `rg`で関数名を探し、テストを1本実行する
5. 「エディタは構造と変更を見る。ターミナルは再現できる命令と出力を見る」と締める

### 話すこと

- ターミナルへ入力した命令はシェルが解釈する
- エディタ内のターミナルも、ターミナルであることは変わらない
- どちらか一方へ乗り換える話ではない

### 失敗時

テストが長引いたら、事前に保存した成功ログへ切り替える。
診断が出なければ、差分表示と定義移動だけを見せる。
3分を超えたら、その場で終了して10枚目へ進む。

## 実演2: 3つの担当へ別の指示を渡す

挿入位置は25枚目のあと。
所要時間は4分。

見せたいことは、3体を起動することではなく、担当と触る場所が重ならないように指示すること。

安全のため、デモでは実装せず、各担当に実装手順だけを考えさせる。

### 事前に用意する3つの指示

UI担当:

```text
読み取り専用。
やること：Todo一覧画面の実装手順を考える
見る場所：画面のフォルダ
見ない場所：APIとDBのフォルダ
終わりの条件：変更候補と確認方法を3行で報告する
```

バックエンド担当:

```text
読み取り専用。
やること：Todoの追加と取得機能の実装手順を考える
見る場所：APIのフォルダ
見ない場所：画面とDBのフォルダ
終わりの条件：変更候補と確認方法を3行で報告する
```

DB担当:

```text
読み取り専用。
やること：Todoを保存する実装手順を考える
見る場所：DBのフォルダ
見ない場所：画面とAPIのフォルダ
終わりの条件：変更候補と確認方法を3行で報告する
```

### 手順

1. 全員が読む設計メモを最初に見せる
2. 3つのセッションへ、担当別の4行を渡す
3. 触る場所が重なっていないことを指す
4. cmuxなどで、3担当が同時に考えている状態を見せる
5. 1件だけ結果を開き、終了条件どおり報告されているか確認する
6. 「レビュー担当は、3件がそろってから動かす」と締める

### 話すこと

- 4体を一斉に動かす必要はない
- 同じファイルを触る指示なら、1体ずつ進める
- 実装する場合は、必要に応じてbranchやworktreeも分ける

### 失敗時

セッションを複数起動できなければ、3つの指示を並べたスクリーンショットを見せる。
ネットワークが不安定なら、事前に保存した結果を1件だけ読む。
4分を超えたら、全件の完了を待たずに26枚目へ進む。

## タイムキープ

| 経過 | 到達点 |
|---|---|
| 0:00 | 表紙 |
| 0:04 | 5枚目、第1章 |
| 0:11 | 9枚目のあと、実演1 |
| 0:14 | 10枚目、ツール比較 |
| 0:26 | 18枚目、第2章 |
| 0:32 | 22枚目、同一プロジェクト |
| 0:40 | 25枚目のあと、実演2 |
| 0:44 | 26枚目、担当のゴール |
| 0:53 | 30枚目、失敗時の確認 |
| 0:55 | 31枚目、まとめ |
| 1:00 | 終了 |

0:26で第2章へ入れていなければ、12〜16枚目は「向く場面」と弱み1点だけを話す。
0:40を超えたら実演2をスクリーンショット1枚に短縮する。
第2章の4担当、同じファイルを避ける話、最後の人間確認は削らない。
