# 実演デモ台本

60分の勉強会で2回入れる。
本番前に必ず通し、同じ解像度でスクリーンショットも用意する。

## 共通の準備

- [ ] 画面共有時のターミナル文字を18pt以上にする
- [ ] 通知、メール、チャットのプレビューを切る
- [ ] 秘密情報、顧客名、個人情報が画面に出ないデモ用リポジトリを使う
- [ ] `git status`で意図しない変更がないことを確認する
- [ ] AIツールへログインできることと利用枠を確認する
- [ ] ネットワーク障害に備え、各デモのスクリーンショットを用意する
- [ ] 書き込みを伴う場合は、使い捨てbranchまたはworktreeを使う

デモでは速さを競わない。
画面上のどの証拠を見て判断したかを、声に出して示す。

## 実演1: 同じプロジェクトを2つの窓で見る

挿入位置は8枚目のあと。
所要時間は3分。

見せたいことは、エディタとターミナルの役割が違い、同時に使えること。

### 手順

1. エディタで小さなデモ用プロジェクトを開く
2. ファイルツリー、定義移動、診断、差分表示を順に指す
3. 統合ターミナルまたは別のターミナルで`pwd`と`git status`を実行する
4. `rg`で関数名を探し、テストを1本実行する
5. 「エディタは構造と変更を見る。ターミナルは再現できる命令と出力を見る」と締める

### 話すこと

- ターミナルへ入力した命令はシェルが解釈する
- エディタ内のターミナルも、ターミナルであることは変わらない
- どちらか一方へ乗り換える話ではない

### 失敗時

テストが長引いたら、事前に保存した成功ログへ切り替える。
診断が出なければ、差分表示と定義移動だけを見せる。
3分を超えたら、その場で終了して9枚目へ進む。

## 実演2: 3つの担当へ別の指示を渡す

挿入位置は27枚目のあと。
所要時間は4分。

見せたいことは、セッションの数ではなく、担当範囲と完了条件を分けて指示すること。

安全のため、デモでは実装せず、各担当に実装手順だけを考えさせる。

### 事前に用意する3つの指示

UI担当:

```text
読み取り専用。
やること：Todo一覧画面の実装手順を考える
主に調べる場所：src/components/
変更対象外：src/api/とsrc/db/
完了条件：変更候補と確認方法を3項目で報告する
```

バックエンド担当:

```text
読み取り専用。
やること：Todoの追加と取得機能の実装手順を考える
主に調べる場所：src/api/
変更対象外：src/components/とsrc/db/
完了条件：変更候補と確認方法を3項目で報告する
```

DB担当:

```text
読み取り専用。
やること：Todoを保存する実装手順を考える
主に調べる場所：src/db/
変更対象外：src/components/とsrc/api/
完了条件：変更候補と確認方法を3項目で報告する
```

### 手順

1. 全員が読む設計メモを最初に見せる
2. 3つのセッションへ、担当別の4項目を渡す
3. 変更対象が重なっていないことを指す
4. cmuxなどで、3担当が同時に考えている状態を見せる
5. 1件だけ結果を開き、完了条件どおり報告されているか確認する
6. 「レビュー担当は、結果が届くたびに確認し、最後に組み合わせる」と締める

### 話すこと

- 4つのセッションを一斉に動かす必要はない
- 同じファイルを変更する指示なら、1つずつ進める
- 実装する場合は、必要に応じてbranchやworktreeも分ける

### 失敗時

セッションを複数起動できなければ、3つの指示を並べたスクリーンショットを見せる。
ネットワークが不安定なら、事前に保存した結果を1件だけ読む。
4分を超えたら、全件の完了を待たずに28枚目へ進む。

## タイムキープ

| 経過 | 到達点 |
|---|---|
| 0:00 | 表紙 |
| 0:03 | 4枚目、第1章 |
| 0:11 | 8枚目のあと、実演1 |
| 0:14 | 9枚目、併用の話 |
| 0:24 | 18枚目、第2章 |
| 0:31 | 22枚目、1つのプロジェクト |
| 0:41 | 27枚目、指示の4項目 |
| 0:44 | 27枚目のあと、実演2 |
| 0:48 | 28枚目、同時編集の問題 |
| 0:53 | 30枚目、まとめ |
| 0:55 | 質疑へ移る |
| 1:00 | 終了 |

0:24で第2章へ入れていなければ、15〜17枚目は結論だけにする。
0:44を超えたら実演2をスクリーンショット1枚に短縮する。
22〜30枚目の担当分け、同時編集を避ける話、最後の人間確認は削らない。
